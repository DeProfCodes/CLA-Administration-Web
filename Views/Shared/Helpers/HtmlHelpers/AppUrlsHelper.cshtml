@using CLACommonFunctionsLibrary_NET.Helpers.Enums
@using CLA_Administration_Web.Helpers.Constants
@using CLA_Administration_Web.Helpers.Enums.AppPages

<script>

    function GetPageUrl(page, id)
    {
        var url = "";

        // Dashboard Section
        if (page == '@DashboardPages.Dashboard') url = '@AppPagesUrl.Dashboard.Home';
        if (page == '@DashboardPages.Calendar') url = '@AppPagesUrl.Dashboard.Calendar';

        // Modules Section
        if (page == '@ModulesPages.ContentLibraryCategories') url = '@AppPagesUrl.Modules.ContentLibraryCategories';
        if (page == '@ModulesPages.ContentLibraryContent') url = '@AppPagesUrl.Modules.ContentLibraryContent';
        //
        if (page == '@ModulesPages.DesktopOverview') url = '@AppPagesUrl.Modules.DesktopOverview';
        if (page == '@ModulesPages.DesktopAddNew') url = '@AppPagesUrl.Modules.DesktopAddNew';
        //
        if (page == '@ModulesPages.LockedDesktopOverview') url = '@AppPagesUrl.Modules.LockedDesktopOverview';
        if (page == '@ModulesPages.LockedDesktopAddNew') url = '@AppPagesUrl.Modules.LockedDesktopAddNew';
        //
        if (page == '@ModulesPages.ScreensaverOverview') url = '@AppPagesUrl.Modules.ScreensaverOverview';
        if (page == '@ModulesPages.ScreensaverAddNew') url = '@AppPagesUrl.Modules.ScreensaverAddNew';
        //
        if (page == '@ModulesPages.PopupOverview') url = '@AppPagesUrl.Modules.PopupOverview';
        if (page == '@ModulesPages.PopupAddNew') url = '@AppPagesUrl.Modules.PopupAddNew';
        //
        if (page == '@ModulesPages.SurveyOverview') url = '@AppPagesUrl.Modules.SurveyOverview';
        if (page == '@ModulesPages.SurveyAddNew') url = '@AppPagesUrl.Modules.SurveyAddNew';
        //
        if (page == '@ModulesPages.TickerOverview') url = '@AppPagesUrl.Modules.TickerOverview';
        if (page == '@ModulesPages.TickerAddNew') url = '@AppPagesUrl.Modules.TickerAddNew';
        //
        if (page == '@ModulesPages.RSSOverview') url = '@AppPagesUrl.Modules.RSSOverview';
        if (page == '@ModulesPages.RSSAddNew') url = '@AppPagesUrl.Modules.RSSAddNew';
        
        // Settings Section
        if (page == '@SettingsPages.StagingUsers') url = '@AppPagesUrl.Setttings.StagingUsers';
        if (page == '@SettingsPages.SetupExclusions') url = '@AppPagesUrl.Setttings.SetupExclusions';
        if (page == '@SettingsPages.AdminAccess') url = '@AppPagesUrl.Setttings.AdminAccess';
        if (page == '@SettingsPages.TargetGroups') url = '@AppPagesUrl.Setttings.TargetGroups';
        if (page == '@SettingsPages.CustomUserSettings') url = '@AppPagesUrl.Setttings.CustomUserSettings';
        if (page == '@SettingsPages.ActiveConnections') url = '@AppPagesUrl.Setttings.ActiveConnections';
        if (page == '@SettingsPages.SkinsOfflineImages') url = '@AppPagesUrl.Setttings.SkinsOfflineImages';
        if (page == '@SettingsPages.DesktopInformation') url = '@AppPagesUrl.Setttings.DesktopInformation';

        // Reports Section
        if (page == '@ReportsPages.Survey') url = '@AppPagesUrl.Reports.Survey';
        if (page == '@ReportsPages.Popup') url = '@AppPagesUrl.Reports.Popup';
        if (page == '@ReportsPages.Ticker') url = '@AppPagesUrl.Reports.Ticker';
        if (page == '@ReportsPages.Policy') url = '@AppPagesUrl.Reports.Policy';
        if (page == '@ReportsPages.ActiveUsers') url = '@AppPagesUrl.Reports.ActiveUsers';
        if (page == '@ReportsPages.ActiveMachines') url = '@AppPagesUrl.Reports.ActiveMachines';
        if (page == '@ReportsPages.CampaignDispatch') url = '@AppPagesUrl.Reports.CampaignDispatch';
        if (page == '@ReportsPages.Troubleshoot') url = '@AppPagesUrl.Reports.Troubleshoot';

        // Support Section
        if (page == '@SupportPages.AboutUs') url = '@AppPagesUrl.Support.AboutUs';
        if (page == '@SupportPages.Tutorials') url = '@AppPagesUrl.Support.Tutorials';
        if (page == '@SupportPages.RemoteAssistance') url = '@AppPagesUrl.Support.RemoteAssistance';
        if (page == '@SupportPages.ContactUs') url = '@AppPagesUrl.Support.ContactUs';
        if (page == '@SupportPages.FAQ') url = '@AppPagesUrl.Support.FAQ';

        // Account Section
        if (page == '@AccountPages.AccountOverview') url = '@AppPagesUrl.Account.AccountOverview';
        if (page == '@AccountPages.SecuritySettings') url = '@AppPagesUrl.Account.SecuritySettings';
        
        // Admin Section
        if (page == '@AdminPages.UploadFiles') url = '@AppPagesUrl.Admin.UploadFiles';
        if (page == '@AdminPages.ApplicationParameters') url = '@AppPagesUrl.Admin.ApplicationParameters';
        if (page == '@AdminPages.Licensing') url = '@AppPagesUrl.Admin.Licensing';
        if (page == '@AdminPages.AutoReminders') url = '@AppPagesUrl.Admin.AutoReminders';
        if (page == '@AdminPages.SQLBrowser') url = '@AppPagesUrl.Admin.SQLBrowser';

        return url;
    }

    function GetPageTitle(page)
    {
       var title = "";

        // Dashboard Section
        if (page == '@DashboardPages.Dashboard') title = "Dashboad";
        if (page == '@DashboardPages.Calendar') title = "Calendar";

        // Modules Section
        if (page == '@ModulesPages.ContentLibraryCategories') title = "Content Library Categories";
        if (page == '@ModulesPages.ContentLibraryContent') title = "Content Library Contents";
        //
        if (page == '@ModulesPages.DesktopOverview') title = "Locked Desktops Overview";
        if (page == '@ModulesPages.DesktopAddNew') title = "Add New Locked Desktop";
        //
        if (page == '@ModulesPages.LockedDesktopOverview') title = "Desktops Overview";
        if (page == '@ModulesPages.LockedDesktopAddNew') title = "Add New Desktop";
        //
        if (page == '@ModulesPages.ScreensaverOverview') title = "Screensavers Overview";
        if (page == '@ModulesPages.ScreensaverAddNew') title = "Add New Screensaver Sequence";
        //
        if (page == '@ModulesPages.PopupOverview') title = "Popups Overview";
        if (page == '@ModulesPages.PopupAddNew') title = "Add New Popup";
        //
        if (page == '@ModulesPages.SurveyOverview') title = "Surveys Overview";
        if (page == '@ModulesPages.SurveyAddNew') title = "Add New Survey";
        //
        if (page == '@ModulesPages.TickerOverview') title = "Tickers Overview";
        if (page == '@ModulesPages.TickerAddNew') title = "Add New Ticker";
        //
        if (page == '@ModulesPages.RSSOverview') title = "RSS Overview";
        if (page == '@ModulesPages.RSSAddNew') title = "Add New RSS";

        // Settings Section
        if (page == '@SettingsPages.StagingUsers') title = "Manage Staging Users";
        if (page == '@SettingsPages.SetupExclusions') title = "Manage Setup Exclusions";
        if (page == '@SettingsPages.AdminAccess') title = "Admin Access";
        if (page == '@SettingsPages.TargetGroups') title = "Manage Targeting Groups";
        if (page == '@SettingsPages.CustomUserSettings') title = "Custom User Settings";
        if (page == '@SettingsPages.ActiveConnections') title = "Manage Active Connections";
        if (page == '@SettingsPages.SkinsOfflineImages') title = "Manage Skins & Offline Images";
        if (page == '@SettingsPages.DesktopInformation') title = "Desktop Information";

        // Reports Section
        if (page == '@ReportsPages.Survey') title = "Survey Report";
        if (page == '@ReportsPages.Popup') title = "Popup Report";
        if (page == '@ReportsPages.Ticker') title = "Ticker Report";
        if (page == '@ReportsPages.Policy') title = "Policy Report";
        if (page == '@ReportsPages.ActiveUsers') title = "Active Users Report";
        if (page == '@ReportsPages.ActiveMachines') title = "Active Machines Report";
        if (page == '@ReportsPages.CampaignDispatch') title = "Campaign Dispatch Listing Report";
        if (page == '@ReportsPages.Troubleshoot') title = "Troubleshoot Report";

        // Support Section
        if (page == '@SupportPages.AboutUs') title = "About CLA";
        if (page == '@SupportPages.Tutorials') title = "CLA Tutorials";
        if (page == '@SupportPages.RemoteAssistance') title = "Remote Assistance";
        if (page == '@SupportPages.ContactUs') title = "Contact Support";

         // Account Section
        if (page == '@AccountPages.AccountOverview') title = "Account Overview";
        if (page == '@AccountPages.SecuritySettings') title = "Security Settings";

        return title;
    }

</script>